'use strict'

var app = app || {};

page('/', somecontroller. indexhtml);
page('/schools', somecontroller to show schools);
page('/projects', somecontroller to show projects);
page('/repos', somecontroller to show repos from GH);
page('/about', some controller to show the about me);
page();
